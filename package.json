{
  "name": "guillaumemaiano2025",
  "version": "1.0.0",
  "description": "Personal website on Azure",
  "main": "scripts.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "clean": "shx rm -rf build",
    "build": "npm run clean && npm run compile-sass && npm run compile-typescript && npm run copy-static && npm run copy-html",
    "compile-sass": "sass scss:build/assets/css",
    "compile-typescript": "tsc && terser build/assets/js/custom.js -o build/assets/js/custom.min.js && shx rm build/assets/js/custom.js",
    "copy-static": "shx cp -r assets/* build/assets/",
    "copy-html": "node scripts/copy.js"
  },
  "devDependencies": {
    "fs-extra": "^11.3.0",
    "glob": "^11.0.3",
    "sass": "^1.89.2",
    "shx": "^0.4.0",
    "terser": "^5.43.1",
    "typescript": "^5.8.3"
  }
}
